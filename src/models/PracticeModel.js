export default class PracticeModel {
    constructor(score, name, value, input, seconds) {
        this.score = score,
        this.name = name,
        this.value = value,
        this.input = input,
        this.seconds = seconds
    }
};