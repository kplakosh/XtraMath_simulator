export default class PracticeModel {
    constructor(score, equation, input, seconds) {
        this.score = score,
        this.equation = equation,
        this.input = input,
        this.seconds = seconds
    }
};