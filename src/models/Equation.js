export default class Equation {
    constructor(name, status, operands, operator, value) {
        this.name = name,
        this.status = status,
        this.operands = operands,
        this.operator = operator,
        this.value = value
    };
};