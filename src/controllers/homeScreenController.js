import { renderChartGrid, displayBtn, operationName } from '../view/homeScreenView';

// Render home screen 
export const render = arr => {
    // Render chart grid
    renderChartGrid(arr);

    // Display button
    displayBtn();
};


